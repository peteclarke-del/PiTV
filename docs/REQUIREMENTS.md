# PiTV requirements checklist

Everything asked for, where it lives, and how it is verified. Kept current with the code;
the design behind each row is in [PLAN.md](PLAN.md).

| # | Requirement | Where | Verified by |
|---|---|---|---|
| 1 | Four general channels: 1 and 2 programmes only, 3 and 4 with adverts in a show, ad, ad pattern | `DEFAULT_CHANNELS` in `db.py`; patterns in `scheduler/build.py` | `test_ads_only_on_ad_channels` |
| 2 | Content from the NAS TV, movie, advert, sport and music video shares; the NAS is read-only | pitv_content's sources, seeded from the installer's share list (`/etc/pitv/nas-sources.json`); its index imported by `catalogue.py`; shares mounted `ro` (`systemd/mnt-share.mount.template`) | `test_import_creates_catalogue_and_is_idempotent`; manual on the Pi |
| 3 | Schedule 08:00 to midnight; overnight replays the day until 08:00 | `rules.day_bounds`, `Builder._overnight` | `test_every_channel_day_is_covered`, `test_no_slot_crosses_into_next_day_start` |
| 4 | Watershed at 21:00 with certificate and kids rules; date- and time-appropriate placement | `rules.allowed_at`, `watershed` and `tv_watershed` settings, dayparts | `test_watershed_respected`, `test_watershed_movie_vs_tv`, `test_kids_cutoff` |
| 5 | A week built ahead; episodes advance from the pilot through the seasons; variety; minimal weekly repeats | horizon build, episode cursors, movie repeat scoring | `test_episodes_in_order_per_show`, `test_movies_spread_evenly`, `test_show_stays_on_home_channel` |
| 6 | No episode of a series directly after another of the same series (sport exempt at weekends) | `last_show_id` rule, overnight tail check | `test_no_same_show_back_to_back` |
| 7 | Subtitles show the episode title, not the season/episode code | `slot_titles`, `episode_subtitle` in `scheduler/build.py` | `test_subtitle_is_episode_title`, `test_slot_titles` |
| 8 | Programmes of any era with a healthy pre/post-1980 mix; adverts 1980s and 1990s only | `era_weights`, `advert_era_weights`, era pool normalisation | `test_healthy_mix_of_eras_and_adverts_only_80s_90s`, `test_default_eras_allow_old_programmes_but_not_old_adverts` |
| 9 | Sport share (wrestling, snooker, motorcycle racing, strongman); weekend blocks modelled on a mid-80s week; back to back allowed at weekends | sport category, weekday, Saturday and Sunday dayparts | `test_weekend_afternoons_carry_sport` |
| 10 | Music channel from the music videos share: blocks by decade (1970s to 2000s) and genre, two concerts a day | `build_music_day`, `music_blocks`, `music_decades` | `test_music_channel_day`, `test_music_channel_decades` |
| 11 | Cartoon channel from animated series; family-safe adverts only | the cartoon channel's genre list and line-up generation; `family_safe` from pitv_content, then its tags, then `adult_advert_keywords` as whole words (`catalogue._family_safe`) | `test_cartoons_routed_to_cartoon_channel`, `test_generation_respects_channel_genres`, `test_family_safe_adverts_on_cartoon_channel`, `test_advert_family_safety_precedence`, `test_advert_keywords_match_whole_words` |
| 12 | Channels can be added, removed, enabled and disabled | channels API, admin Channels tab | `test_channels_and_settings` |
| 13 | Fast-booting Pi 4 with minimal overhead; hardware H.264 decode | `setup/boot-trim.sh`, `splash.py`, `player/hwdec.py` | manual on the Pi |
| 14 | OSMC RF remote: channel select and steps, volume, mute, pause and restart, guide with up/down/left/right (left no further back than now); keys reassignable by pressing them | `player/input.py`, guide navigation in `player/controller.py`, keymap editor on the admin Player tab | manual on the Pi |
| 15 | Real-time programme guide on screen that agrees with the web guide | `player/osd.py`, 30 s refresh; lookups shared in `guide.py` | screenshots, `test_guide_collapses_music_blocks` |
| 16 | 14" 4:3 CRT through HDMI-to-SCART at PAL 576p; overlays overscan-safe | `DISPLAY_MODE=hdmi576` in `boot-trim.sh`, `display_aspect`, `osd_safe_margin`, `osd_scale` | screenshots at 768x576 |
| 17 | Web interface: schedule view and admin for sources, channels, schedules, weighting, ads on/off and patterns; fast, reactive, clean | FastAPI and Svelte (`pitv/web`, `web/`); sources through pitv_content (row 39) | `tests/test_api.py`; browser checks by hand |
| 18 | Virtual remote on the web and from the API | `#/remote`, `POST /api/player/key`, `/channel`, `/volume`, control socket | `test_player_offline`; manual |
| 19 | Admin log viewer; the player logs failures, timeouts and the current file, codec and stream | `logsetup.py`, `/api/logs`, `/api/logs/{name}` (player, web, catalogue, schedule, install), `/api/logs/journal/{unit}`, stream info lines in `controller.py` | `test_content_tool_proxy_non_json_and_log_shape` (log shape); manual |
| 20 | Install log from the SD-card installer and first boot readable in the admin | `/work/install/install.log`, Logs tab source "install" | manual (first boot on the Pi) |
| 21 | Local HDD cache; a day's television cached before 08:00 | request manifest through the next broadcast day; pitv_content fills it; `player/cache.py` evicts | `test_content_manifest_and_report`, `test_content_manifest_ignores_part_files`, `test_cache_located_items_count_as_cached` |
| 22 | Missing programmes are requested from pitv_content, which fetches, encodes to 576p and places them in the cache; downloads never go to the NAS | wanted list and admin Wanted page, manifest `fetch` requests and `wanted` with `dest_dir` under `acquire_dir` | `test_wanted_list`, `test_gap_detection`, `test_fetched_titles_cannot_leave_the_acquire_folder`, `test_request_already_in_the_library_is_met_by_that_file`, `test_gap_episode_is_filed_under_its_series` |
| 23 | Readiness: check in plenty of time; substitute and rebalance missing programmes; flag errors in the logs | `readiness.py` at `readiness_hours` (06:00, 07:00) and `POST /api/content/readiness`: cached is ready, NAS fallback is a warning, anything else is an error and the channel-day is rebuilt from what is playable now. No substitution at air time | `test_readiness_substitutes_missing_file`, `test_readiness_substitutes_unfetched_placeholders`, `test_content_tool_status` |
| 24 | Clear split with pitv_content; both on the Pi sharing the cache; synchronised | `docs/CONTENT_CONTRACT.md` (schema 2); `catalogue.py` (index), `content.py` (manifest and reports); `.part` and marker rules in `player/cache.py`; PLAN.md section 7 | `test_content_manifest_and_report`, `test_content_manifest_ignores_part_files`, `test_rejects_other_schemas`, `test_malformed_records_are_rejected_and_counted`, `test_malformed_report_entries_are_not_fatal`, `test_report_file_already_posted_is_not_applied_twice`, `test_pitv_content_uses_its_token_not_a_session`, `test_make_room_evicts_only_unprotected_cache_copies` |
| 25 | pitv_content's status, log and runs visible and controllable from this admin | `GET /api/content/tool`, `POST /api/content/tool/run`, `GET /api/content/tool/log`, admin Content page | `test_content_tool_status` |
| 26 | Everything moved to pitv_content (sources, settings, providers, catalogue, jobs) is controllable from this admin | `GET/PUT /api/sources`; proxy `/api/content/tool/api/*`, Content page schema form | `test_content_tool_proxy_offline`, `test_content_tool_proxy_non_json_and_log_shape`, `test_sources_are_pitv_contents` |
| 27 | No zombie or duplicated fetch, encode or indexing code in PiTV | `pitv/acquire` removed; the NAS scanner (`pitv/library/`: scanner, naming, NFO, ffprobe) and `probe_cache` removed | grep in review |
| 28 | Watchdog, no memory leaks, 24/7; recover from power loss to the current time | `sdnotify.py`, unit hardening, hardware watchdog, clock wait and jump handling | manual on the Pi |
| 29 | Desktop preview window shows the Pi's picture (4:3 PAL) so transcodes can be judged | windowed mpv args in `mpv_ipc.default_args`, `setup/dev.sh` | manual |
| 30 | Settings and overrides exportable for backup | `GET /api/export`, System tab | manual |
| 31 | SD-card installer (Linux and Windows): system and work partitions, clean/normal/upgrade modes, first-boot installation of both apps, Wi-Fi and maintenance user | `installer/` (Go CLI, DietPi image build, first-boot script); the share list reaches pitv_content as `NAS_SOURCES` | `installer/cli` Go tests; first boot on the Pi |
| 32 | Each channel carries only programmes that suit it, driven by configuration in the admin, never by channel names or numbers in code | per-channel allowed and excluded genres, `lineup.channel_fit` and `generate` | `test_generation_respects_channel_genres` |
| 33 | A programme belongs to one channel only; a series on one channel is never scheduled on another | `lineup` table, derived `home_channel_id`, scheduler candidate filter | `test_every_programme_belongs_to_exactly_one_channel`, `test_week_never_shares_a_programme_across_channels`, `test_show_stays_on_home_channel` |
| 34 | Admin can add and remove programmes on a channel from a dropdown, including titles not on disk | line-up drawer, `GET /api/lineup/options`, `POST/PUT/DELETE /api/lineup` | `test_move_and_remove_entries`, `test_lineup_api` |
| 35 | NAS-only switch: schedule only what is on the NAS or in the cache, or also schedule line-up entries that pitv_content will fetch | `nas_only` setting and channel override, external placeholders, `external_lead_days` | `test_external_entry_scheduled_ahead_and_requested` |
| 36 | Transient content: fetched material lives only in the cache and is removed after airing | `transient`, `remove_after_airing`, `transient_keep_days`, `lineup.remove_aired_transients` | `test_transient_removal_never_leaves_the_cache`, `test_delivery_report_fills_placeholder` |
| 37 | Fetched files take their scheduled slots; missing ones are substituted in time | `content.apply_report` creates the catalogue entry from the report's `meta`, `lineup.attach_delivery` binds it; readiness placeholder checks | `test_delivery_report_fills_placeholder`, `test_readiness_substitutes_unfetched_placeholders` |
| 38 | Line-ups kept as a configuration file that survives a database rebuild and can be edited offline | `lineups.json` mirror, `GET /api/lineup/export`, `POST /api/lineup/import`, restore on the next catalogue import | `test_move_and_remove_entries`, `test_lineup_api` |
| 39 | Sources are owned by pitv_content and shown and edited in PiTV's admin; read-only from the last index when pitv_content is down | `GET/PUT /api/sources` in `web/api/admin.py` (through pitv_content's API), `sources` mirror table, admin Sources page | `test_sources_are_pitv_contents` |
| 40 | The scheduler draws from a catalogue that starts from the NAS and can be extended with custom programming | `catalogue.py` imports the schema 2 index (`pitv catalogue`, `POST /api/catalogue/refresh`, `/import`, 04:00 and on index change); line-up external entries; online deliveries become catalogue rows | `test_import_creates_catalogue_and_is_idempotent`, `test_complete_index_marks_absent_items_missing`, `test_old_scan_rows_are_adopted`, `test_catalogue_import_endpoint`, `test_delivery_report_fills_placeholder` |
| 41 | Every scheduled programme is requested from pitv_content: copied or transcoded from the NAS, or fetched online | `content.manifest`, `GET /api/content/manifest`, `pitv content-manifest` | `test_content_manifest_and_report` |
| 42 | Playback from the cache first; the NAS original only with `nas_fallback` on; otherwise the technical difficulties card, logged and retried every 30 s | `MediaCache.locate` in `player/cache.py`; `play_live` and `_technical_difficulties` in `player/controller.py` | `test_playback_chain`; manual on the Pi |
| 43 | The admin makes clear which app each page and setting affects | PiTV and pitv_content sections in the admin (PLAN.md section 6.3; frontend in progress) | browser check by hand |
| 44 | Every service of both apps listed with its status in Admin, System | `web/api/services.py` (one `systemctl show` plus a live check per process), System page; sudoers rule in `setup/install.sh` | `test_services_cover_both_apps`, `test_service_actions_match_sudoers` |
| 45 | Admin, System shows each app's version and host details, correct when the apps run on separate machines | `pitv/hostinfo.py`; pitv_content's `GET /api/system` (contract section 7) through the proxy; `HostCard` on the System page | `test_system_reports_host_in_the_contract_shape`; checked against pitv_content's endpoint by hand |
| 46 | The admin shows settings and pages by familiarity: Basic, Standard, Advanced | `pitv/settings_schema.py` levels, `LevelSwitch`, `Tabs`, `SchemaForm`; section levels in `Admin.svelte`, `Content.svelte`, `ChannelEditor.svelte` | `test_settings_schema_matches_the_defaults`; browser check by hand |
| 47 | Large admin pages are split into panes and sections | Settings panes from the schema, channel editor sections, catalogue tabs | browser check by hand |
| 48 | Titles can be added to the catalogue from the admin | `AddToCatalogue.svelte` on the Catalogue page; `POST /api/lineup` without a channel places it by genres (`lineup.best_channel`); adverts and music videos through `POST /api/wanted` | `test_added_title_without_a_channel_goes_where_its_genres_fit` |
| 49 | Every table of records pages, filters and sorts by any column, with movable columns, consistently | `DataTable.svelte`, `Pager.svelte`, `lib/table.js`, per-table preferences in `lib/prefs.svelte.js` | browser check by hand |
| 50 | Titles added from the admin are looked up online first so the right one is added and fetched | pitv_content's `GET /api/lookup` through the proxy; `LookupResults.svelte`; `lineup.match` carried in the manifest (contract sections 2 and 8) | `test_external_entry_scheduled_ahead_and_requested`, `test_confirmed_identity_is_cleaned_and_survives_the_mirror`; live check against TVmaze |
